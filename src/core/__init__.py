"""
Core module containing essential game systems.

Provides the main game loop (Game), state management (State, StateManager),
collision resolution (CollisionSystem), logging (logger), profiling tools,
and save/load functionality.
"""